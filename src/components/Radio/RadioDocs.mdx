import { Meta, Title, Controls, Story, Primary, Canvas, Stories } from '@storybook/blocks';
import * as RadioStories from './Radio.stories';

<Meta
   of={RadioStories}
   name="Docs"
/>

<Title />

The `Radio` component is a component that allows users to select one option from a list of options. It is a part of the `RadioGroup` component.

### Import 
```ts
import { Radio } from '@component/Radio/Radio';
```

### API
*Note: **Click onto the component name** to view its props and events document*
| Component | Description |
| - | - |
| [`<Radio>`]() | Component used to render the radio options |
| [`<RadioGroup>`]() | Component used to render set of `Radio` options |

### Usage
<Primary />
<Controls />

<Stories />