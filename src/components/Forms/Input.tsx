import {computed, defineComponent} from "vue"

const Input = defineComponent({
    name: "Input",
    props:{},
    setup(props,{slots,attrs}){
        return ()=>{
            return(
                <>
                    
                </>
            )
        }
    }
})