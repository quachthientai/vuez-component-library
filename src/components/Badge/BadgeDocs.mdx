import { Meta, Title, Primary, Controls, Stories, ArgTypes, Source } from '@storybook/blocks';
import * as BadgeStories from './Badge.stories';

<Meta 
   of={BadgeStories}
   name="Docs"
/>

<Title />

The `Badge` a small component is used to displaying a small numerical value or status descriptor 

### Import
```ts
   import { Badge } from '@/components/Badge';
```

### Usage
<Primary />
<Controls exclude={['Default']}/>

<Stories />

### Slots
<ArgTypes include={'Default'}/>






